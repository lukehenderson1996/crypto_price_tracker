#updated for python 3 usage
import requests, json
import time
from time import sleep, localtime, strftime
import sys
import os
import logging


lastRun = 0 #time.time() #time in seconds, when files were last managed

while true:
    if true: #if it's just past midnight and its been 2 hours since last run
        #
    else:
        sleep(600) #sleep 10 min
